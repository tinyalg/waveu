version: "0.1.0"
description: Start And Stop Example

dependencies:

# The component name without namespace must match the name of the top level directory.
  tinyalg/waveu:
    override_path: '../../..'
