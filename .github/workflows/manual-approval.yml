name: Require Manual Approval for Pushes to Main

on:
  push:
    branches:
      - main

jobs:
  manual-approval:
    name: Await Manual Approval
    runs-on: ubuntu-latest
    environment:
      name: main-approval
      # The job waits for manual approval here
    steps:
      - name: Request Approval
        run: echo "Awaiting manual approval for push to main branch."
